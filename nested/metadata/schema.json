{
  "version": "1.0",
  "schemas": {
    "concept": {
      "required": ["title", "description", "tags", "relationships"],
      "properties": {
        "title": "string",
        "description": "string",
        "tags": "array",
        "relationships": "array"
      }
    },
    "implementation": {
      "required": ["title", "type", "inputs", "outputs"],
      "properties": {
        "title": "string",
        "type": "string",
        "inputs": "array",
        "outputs": "array"
      }
    },
    "template": {
      "required": ["name", "version", "parameters"],
      "properties": {
        "name": "string",
        "version": "string",
        "parameters": "object"
      }
    }
  }
}
